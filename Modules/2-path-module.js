const path = require('path');

console.log(path.sep)

//const filePath = path.join('/folder','subfolder',filename') //will return the whole path
//const base = path.basename(filePath)  will return the file name
//const absolute=path.resolve(__dirname,'folder','subfolder','filname') will give the absolute path as __dirname will point to the current directory